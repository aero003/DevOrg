name: 'Hello World 2.0' 
description: 'Greet someone'
inputs:
  SF_USERNAME:  # id of input
    description: 'Enter the Username'
    required: true
runs:
  using: "composite"
  steps:
    - run: |
        echo "Without ENV Variable: ${{ inputs.SF_USERNAME }}"
        echo "With ENV  Variaeble: $username"
        echo "::set-env name=APP_VERSION::${{ inputs.SF_USERNAME }}"
        bash goodbye.sh
      shell: bash  
      env:
        username: ${{ inputs.SF_USERNAME }}
